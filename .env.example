# Alchemy API Configuration (Optional if ETH_RPC_URL is set)
ALCHEMY_API_KEY=your_alchemy_api_key_here
# Public RPC URLs for ETH and BTC
ETH_RPC_URL=https://eth.llamarpc.com
BTC_RPC_URL=https://bitcoin-rpc.publicnode.com

# Database (PostgreSQL via Docker)
# Run: docker-compose up -d
DATABASE_URL=postgresql://postgres:postgres123@localhost:5434/crypto_indexer

# Redis Caching
REDIS_URL=redis://localhost:6379

# Server Configuration
PORT=5000
